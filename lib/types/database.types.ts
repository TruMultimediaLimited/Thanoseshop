/**
 * Placeholder Supabase database types.
 *
 * Real types will be generated from the live schema once a Supabase project
 * exists, via:
 *   npx supabase gen types typescript --project-id <ref> > lib/types/database.types.ts
 *
 * Until then this permissive shape keeps the Supabase clients type-checking.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
